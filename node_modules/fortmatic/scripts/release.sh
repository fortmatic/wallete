#!/usr/bin/env bash

# Building
echo ----- Building -----
npm run build

# Push version tagging
echo ----- Push version -----
npm version patch || exit 1
git push origin master --tags || exit 1

# Publish to NPM
echo ----- Publishing -----
npm publish

# Revert local changes
git checkout .
